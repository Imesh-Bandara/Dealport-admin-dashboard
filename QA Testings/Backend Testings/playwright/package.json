{
  "name": "dealport-backend-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright API test suite for the Dealport backend (NestJS + Prisma). Run from this folder with `npx playwright test`.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:regression": "playwright test --grep @regression"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^26.1.2"
  }
}
