# dependencies
/node_modules

# build output
/dist

# env files (never commit real secrets — see .env.example)
.env
.env.*
!.env.example

# testing
/coverage

# misc
*.tsbuildinfo
.DS_Store
npm-debug.log*

# uploaded product images (local dev storage — not source)
/uploads/*
!/uploads/.gitkeep
